import React from "react";
import { Button } from "antd";

const HomePage = () => {
  return (
    <div>
      HomePage new app
      <Button>Ant Button</Button>
    </div>
  );
};

export default HomePage;
